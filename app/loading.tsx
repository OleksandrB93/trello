import Loading from "@/components/loading";

export default function LoadingPage() {
  return (
    <div className="absolute bottom-0 top-0 w-full flex justify-center items-center">
      <Loading />
    </div>
  );
}
